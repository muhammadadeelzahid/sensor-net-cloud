services:
  - type: web
    name: sensor-net-cloud-api
    runtime: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sensor-net-db
          property: connectionString

databases:
  - name: sensor-net-db
    plan: free
